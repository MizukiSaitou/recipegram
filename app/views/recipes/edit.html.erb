
<%= form_with model: @recipe, local:true do |f| %>

<%= f.label:title, "レシピ名" %>
<%= f.text_field:title %>

<%= f.label:body, "作り方" %>
<%= f.text_area:body %>

<%= f.label:image, "写真" %>
<%= f.attachment_field:image %>

<%= f.submit '投稿' %>

<% end %>