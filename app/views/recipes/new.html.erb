<%= form_with model: @recipe, local:true do |f| %>

<%= f.label:title, "レシピ名" %>
<%= f.text_field:title %>

<%= f.label:body, "作り方" %>
<%= f.text_area:body %>

<%= f.label:image, "画像" %>
<%= f.attachment_field:image %>

<%= f.submit %>

<% end %>